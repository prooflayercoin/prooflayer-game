{
  "buildCommand": "pnpm --filter @prooflayer/web build",
  "outputDirectory": "apps/web/.next",
  "framework": "nextjs",
  "env": {
    "NEXT_PUBLIC_API_URL": {
      "description": "API base URL for game server",
      "default": "https://api.prooflayer.app"
    }
  }
}
